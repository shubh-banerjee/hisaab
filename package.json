{
  "name": "hisaab",
  "version": "1.0.0",
  "description": "AI-powered what-if simulator for retail business owners",
  "main": "server.js",
  "scripts": {
    "postinstall": "node scripts/apply-runtime-fixes.js && node scripts/apply-question-scope-fixes.js",
    "start": "node scripts/apply-runtime-fixes.js && node scripts/apply-question-scope-fixes.js && node server.js",
    "dev": "node --env-file=.env --watch server.js",
    "seed:demo-decisions": "node --env-file=.env scripts/seed-demo-decisions.js"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "firebase-admin": "^14.1.0"
  }
}